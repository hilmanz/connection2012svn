
<div id="badgeAuction">
    <div id="content">
    	<a class="post postBadgeAuctions" href="#postBadgeAuction"></a>
    	<div class="post postBadgeAuctionChoose" id="postBadgeAuction" style="display:none;">
        	{*}
        	<div class="badgeAuctionList" style="width:250px;">
				{assign var="rows" value=0}
				{section name=i loop=$have max=7}
				{assign var="rows" value=$rows+1}
                <div class="boxBadges">
                    <div class="theBadges" style="text-align:center"><a href="index.php?page=badges&act=auction_bid&id={$have[i]->id}"><img src="img/auction/{$have[i]->img}" style="height:49px;width:53px" /></a></div>
                </div>
				{if $rows%2 == 0 }<div style="clear:both"></div>{/if}
               {/section}
            </div>
            {/*}
            <div class="entry" style="font-size:16px; left:40px; top:175px; width:347px;">
            	<p>Going, going,...gone!</p><p> The auction items have all been claimed. Well done to the highest bidders</p><p>Remember we are still looking for travellers - complete the badges set and we could meet you at the boarding gate
!</p>
            </div>
            <div id="yesterdayWinner" class="smallBox">
            	<div class="yesterdayWinner" >
                    <h1>Highest Bidder</h1>
                    <div class="theBadges" style="text-align:center"><img {if $lastestWinner.bid}src="img/auction/{$lastestWinner.auction->img}"{else}src="img/badge_auction.png"{/if}  style="height:53px;width:56px" /></div>
                    <div class="boxPoin">
                    	{if $lastestWinner.bid}<h3>{$lastestWinner.bid} <small>POINTS</small></h3>{/if}
                    </div><!-- end .boxPoin -->
                    <h2>Claimed by</h2>
                    <div class="boxTrader">
                        <div class="thumbTrader">
                            <a {if $lastestWinner.bid}href="#popupProfile" class="popProfile" onCLick="getUserProfile({$lastestWinner.user->registerid})"{/if} ><img  {if $lastestWinner.user->photourl=='' }{if $lastestWinner.user->sex== 'F'}src="avatar/thumb/small_avatar/avatar-woman.jpg"{else}src="avatar/thumb/small_avatar/avatar-man.jpg"{/if}{else}src="{$lastestWinner.user->photourl}"{/if} /></a>
                        </div><!-- end .thumbTrader -->
                        {if $lastestWinner.bid}
						<div class="boxName">
                            <span class="nameTrader">{$lastestWinner.user->name}</span>
                        </div><!-- end .boxName -->
						{/if}
                    </div><!-- end .boxTrader -->
                    {if $lastestWinner.bid}<a href="?page=badges&act=auction_winner" class="viewAllWinner">View All &raquo;</a>{/if}
                </div><!-- end .yesterdayWinner -->
            </div><!-- end #yesterdayWinner -->
 		</div><!-- end .post -->
    </div><!-- end #content -->
</div><!-- end #activity -->
{include file="marlboro/profile.html"}

{literal}
<script>
function getUserProfile(id){
$("#loadingProfile").show();
$(".poupContent").hide();
$.post("index.php?page=myprofile&act=profilePopup", { register_id: id},
			   function(data) {
					$("#loadingProfile").hide();
					$(".poupContent").show();
					var html='';
					$("#usernameID").html(data.name); 
					$("#ageID").html(data.age); 
					$("#townsID").html(data.kota); 	
					$("#connectDateID").html(data.date); 
					$("#badgesValuesID").html(data.totalBadge); 
					$("#imageAvatar").attr('src',data.avatar_sm);
					$("#hasTradeCount").html(""); 
					$("#headerNameProfile").html(data.name+" PROFILE"); 
					for (var i in data.badge){
						html += "<div class='badgeBox'>";
                        html += '<a class="badgesIcon tip_trigger" href="#"><img src="img/badge/'+data.badge[i].image+'" /><span class="tip"><span class="arrow"></span>Vulputate Ornare Dolor Risus</span></a>';
                        html += '<div class="badgesCount">'+data.badge[i].total+'</div>';
                        html += '</div>';
					
					}
					var auctionReward ='';
					for (var j in data.auctionwin){
						auctionReward += '<a class="piala tip_trigger" href="#"><span class="tip"><span class="arrow"></span><p> '+data.name+' WIN 1 IPAD</p><img src="img/auction/'+data.auctionwin[j].auction_detail.img+'" /></span></a>';
                    }
					$("#piala").html(auctionReward);
					$("#badgeList").html(html);
					$("#popupProfile").fadeIn();
					$("#bgPopup").fadeIn();
		});


}


</script>
{/literal}